{
  "name": "nouvaris",
  "version": "1.0.0",
  "private": true,
  "description": "Nouvaris - AI Agent Platform with Dashboard",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "lint": "npm run lint --workspace=frontend"
  },
  "engines": {
    "node": ">=18"
  }
}
